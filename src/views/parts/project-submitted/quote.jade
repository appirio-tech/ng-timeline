li.quote(
  ng-class="{ completed: vm.events.quote.created, passed: vm.events.payment.created }"
  ng-show="vm.expanded.submitted"
)
  section.checkpoint
    .progress
      .percent 15%

      .status Complete

      .day Day 1

    .lead

    .point

    .lead-after

    p(ng-hide="vm.events.quote.created") Generate Quote

    p(ng-show="vm.events.quote.created")
      .carat

      | Quote has been generated.

  p(ng-show="vm.events.quote.created && !vm.events.payment.created")
    a(href="#") View quote and pay to continue.

  .expandable-content
    button(type="button").clean.close x

    ul
      li
        label Cost:
        | 
        | ${{ vm.events.quote.price }}

      li
        label Time:
        | 
        | {{ vm.events.quote.duration }} Days

    ul
      li.accept
        button Accept

      li
        button Reject