h2 Congratulations!

time (wip)

event-card-main
  p You have chosen {{event.userProfiles.length}} winner(s)

  ul.avatars.flex.center
    li(ng-repeat="user in event.userProfiles track by $index")
      avatar(avatar-url="{{user.avatar}}")