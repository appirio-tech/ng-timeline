li.notification.messages.no-progress.completed
  section.checkpoint
    .point.icon
      button.count.clean(ng-click="vm.showMessagingWidget = true")
        .notification 6

    .lead-after

    label
      img.avatar(ng-src="{{ vm.avatars[member.handle] }}")

      | Maybe it's best if we stick with the current logo and Maybe it's best if we stick with the current logo and

  ul.links
    li
      button.clean(ng-click="vm.showMessagingWidget = true") View

    li
      a(ui-sref="messaging({id: 123})") View full thread

  .expandable-content(ng-class="{active: vm.showMessagingWidget}")
    button.clean.close(ng-click="vm.showMessagingWidget = false") x

    messaging.widget(thread-id="123" subscriber-id="Batman")
